version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2_user/job_crawler

hooks:
  ApplicationStop:
    - location: scripts/application_stop.sh
      timeout: 300
      runas: ec2_user
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: ec2_user
